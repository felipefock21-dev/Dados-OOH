{
  "version": 1,
  "include": ["/*"],
  "exclude": ["/api/*"],
  "routes": [
    {
      "pattern": "/api/*",
      "handler": "worker"
    },
    {
      "pattern": "/*",
      "handler": "filesystem"
    }
  ]
}
